import * as React from 'react';

const styles = {
  
}

export interface PlaceholderProps {
  some: string
}

export default class IconPlaceHolder extends React.Component<PlaceholderProps, {}> {
  render() {
    return(
      <div>Hello IconPlaceHolder</div>
    )
  }
}